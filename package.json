{
  "name": "skillops",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "test": "npm run build && node --test dist/tests/*.js",
    "typecheck": "tsc --noEmit",
    "build": "tsc"
  },
  "dependencies": {},
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
