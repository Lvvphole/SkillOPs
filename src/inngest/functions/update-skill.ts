import {writeFileSync,mkdirSync} from 'node:fs';import {dirname} from 'node:path';import {approveSkillUpdate} from '../../verification/approveSkillUpdate.js';import {inngest} from '../client.js';
export const updateSkill=inngest.createFunction({id:'update-skill'},{event:'skill/update'},async({event}:any)=>{const approval=approveSkillUpdate(event.data.change);if(!approval.approved)return {updated:false,approval};const path=`src/skills/${event.data.change.skillId}/versions/${event.data.change.toVersion}.skill.md`;mkdirSync(dirname(path),{recursive:true});writeFileSync(path,event.data.change.proposedContent);return {updated:true,path,approval};});
