import test from 'node:test';import assert from 'node:assert/strict';import {approveSkillUpdate} from '../src/verification/approveSkillUpdate.js';
test('rejects unversioned update and approves evidence-backed version',()=>{const evidencePath='artifacts/verification-report.md';assert.equal(approveSkillUpdate({skillId:'incident-triage',fromVersion:'v1',toVersion:'v1',proposedContent:'x',evidencePaths:[evidencePath],reason:'test'}).approved,false);assert.equal(approveSkillUpdate({skillId:'incident-triage',fromVersion:'v1',toVersion:'v3',proposedContent:'x',evidencePaths:[evidencePath],reason:'test'}).approved,true);});
